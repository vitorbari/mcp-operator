apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Monitoring overlay for MCP Operator
# This adds Prometheus ServiceMonitor and Grafana Dashboard resources
# Requires Prometheus Operator to be installed in the cluster

namespace: mcp-operator-system
namePrefix: mcp-operator-

resources:
- ../prometheus
- ../grafana
