apiVersion: v1
kind: Service
metadata:
  name: mcp-test
  labels:
    app: mcp-test
spec:
  selector:
    app: mcp-test
  ports:
    - name: mcp
      port: 8080
      targetPort: 8080
      protocol: TCP
    - name: metrics
      port: 9090
      targetPort: 9090
      protocol: TCP
