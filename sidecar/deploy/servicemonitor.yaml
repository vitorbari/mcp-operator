apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mcp-test
  labels:
    app: mcp-test
spec:
  selector:
    matchLabels:
      app: mcp-test
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
